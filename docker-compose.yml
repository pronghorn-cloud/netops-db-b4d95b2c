version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=localhost:5432
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRE=7d
    restart: unless-stopped

volumes:
  app_data:
